{
    "cmd": ["npx", "stylelint", "--formatter", "unix", "--fix", "$file"],
    "file_regex": "^(\\S[^:]+):([0-9]+):([0-9]+): (.*)$",
    "working_dir": "$folder",
    "selector": "source.scss, source.less, source.css"
}
